mixin features(data)
  .gfp24_features(class=data.modifiers)
    if data.items
      each item in data.items
        h3.gfp24_features-item-title= item.title
        if item.text
          p.gfp24_features-item-text= item.text
